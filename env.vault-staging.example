# Vault Staging Configuration (NVIDIA)
# Copy to .env.vault-staging and source: source .env.vault-staging

# NVIDIA Staging Vault (not secret - just server addresses)
VAULT_ADDR=https://stg.internal.vault.nvidia.com
VAULT_NAMESPACE=wwfo-self-ta

# IMPORTANT: Only VAULT_TOKEN is secret! Get it from:
# ./scripts/vault/get_vault_token.sh staging
VAULT_TOKEN=your-staging-token-here

# Optional settings
VAULT_MOUNT_POINT=secret
USE_VAULT=true

# Environment flag (enforces Vault requirement)
ENVIRONMENT=staging

# Note: Staging is for testing before production deployment

