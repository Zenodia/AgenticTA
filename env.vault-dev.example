# Vault Development Configuration
# Copy to .env.vault-dev and source: source .env.vault-dev

# Local Vault (development only)
VAULT_ADDR=http://vault-dev:8200
VAULT_TOKEN=dev-root-token-agenticta
VAULT_NAMESPACE=

# Optional: Explicitly enable Vault mode
USE_VAULT=true

