# Vault Production Configuration (NVIDIA)
# Copy to .env.vault-prod and source: source .env.vault-prod

# ⚠️ WARNING: This is PRODUCTION! Test in staging first!

# NVIDIA Production Vault (not secret - just server addresses)
VAULT_ADDR=https://prod.internal.vault.nvidia.com
VAULT_NAMESPACE=wwfo-self-ta

# IMPORTANT: Only VAULT_TOKEN is secret! Get it from:
# ./scripts/vault/get_vault_token.sh prod
VAULT_TOKEN=your-production-token-here

# Optional settings
VAULT_MOUNT_POINT=secret
USE_VAULT=true

# Environment flag (CRITICAL: Enforces Vault requirement!)
ENVIRONMENT=production

# Production: Enable token auto-renewal for long-running services
VAULT_TOKEN_RENEW=true

